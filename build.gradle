buildscript {
    ext.plugin = [
            android: [
                    app: 'com.android.application',
                    lib: 'com.android.library'
            ],
            kotlin : [
                    android: 'kotlin-android',
                    kapt   : 'kotlin-kapt'
            ],
            novoda : 'com.novoda.bintray-release'
    ]
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.2'
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.1.2-3'
        classpath 'com.novoda:bintray-release:0.3.4'
    }
}

allprojects {
    ext.config = [
            sdk       : [
                    'min'   : 9,
                    'target': 25
            ],
            buildTools: '25.0.2',
            testRunner: 'android.support.test.runner.AndroidJUnitRunner'
    ]

    ext.deps = [
            junit  : 'junit:junit:4.12',
            support: [
                    compat   : 'com.android.support:support-compat:25.3.1',
                    appcompat: 'com.android.support:appcompat-v7:25.3.1',
                    test     : [
                            runner  : 'com.android.support.test:runner:0.5',
                            espresso: 'com.android.support.test.espresso:espresso-core:2.2.2'
                    ]
            ],
            kotlin : 'org.jetbrains.kotlin:kotlin-stdlib-jre7:1.1.2-3'
    ]
    repositories {
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

ext.bintray = [
        user    : 'hendraanggrian',
        group   : 'com.hendraanggrian',
        artifact: 'support-utils',
        version : '0.4',
        desc    : 'Sets of utility class for Android.',
        website : 'https://github.com/HendraAnggrian/support-utils'
]

//$ ./gradlew bintrayUpload -PbintrayUser=hendraanggrian -PbintrayKey= -PdryRun=false